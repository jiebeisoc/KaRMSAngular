import { FoodOrderTransactionLineItem } from './food-order-transaction-line-item';

describe('FoodOrderTransactionLineItem', () => {
  it('should create an instance', () => {
    expect(new FoodOrderTransactionLineItem()).toBeTruthy();
  });
});
