import { FoodOrderTransaction } from './food-order-transaction';

describe('FoodOrderTransaction', () => {
  it('should create an instance', () => {
    expect(new FoodOrderTransaction()).toBeTruthy();
  });
});
